# ApexQuant 0.0.1 需求规格说明书

| 项目 | 内容 |
| --- | --- |
| 文档编号 | SRS-0.0.1 |
| 文档状态 | Implementation Baseline |
| 产品 | ApexQuant 个人 A 股量化研究与模拟交易系统 |
| 适用版本 | 0.0.1 |
| 编写日期 | 2026-08-13 |
| 上游文档 | `REQUIREMENTS-0.0.1.md`、`DESIGN.md`、`UI-DESIGN.md`、`PRD-0.0.1.md` |
| 原型基线 | Open Design 项目「ApexQuant 原型设计」 |

## 1. 文档目的

本文定义 ApexQuant 0.0.1 的可实现、可测试和可验收要求，作为产品、设计、前端、后端、数据和测试人员之间的共同契约。

当本文与其他文档发生冲突时，按以下顺序处理：

1. 本文的功能、状态和验收要求；
2. `REQUIREMENTS-0.0.1.md` 的字段级需求；
3. `DESIGN.md` 的视觉、组件和可访问性要求；
4. `UI-DESIGN.md` 的页面信息架构；
5. `PRD-0.0.1.md` 中未冲突的业务背景和规则。

## 2. 产品范围

### 2.1 本版本包含

- 数据集查看、质量检查、补数和数据版本管理；
- 策略配置、生命周期管理、数据泄漏检查和样本外评估入口；
- 可复现回测配置、运行摘要、指标、交易明细和实验对比；
- 日频/分钟级模拟执行、订单状态、风险门禁和审计链路；
- 模拟账户资产、持仓、T+1、风险限额和模拟强平；
- 运行记录、错误诊断、CLI 重跑命令和审计事件；
- 交易规则、费用模型、模拟默认值和通知配置；
- 桌面优先的深色控制室界面，支持窄屏和移动端降级浏览。

### 2.2 本版本明确不包含

- 不连接真实券商柜台，不发送真实委托；
- 不承诺免费行情达到实盘级实时性；
- 不实现复杂代码编辑器、自动机器学习搜索或生产级智能路由；
- 不覆盖多用户权限、组织协作和云端账户体系；
- 不把原型演示数据当作真实交易结果。

### 2.3 关键默认值

| 参数 | 默认值 |
| --- | --- |
| 时区 | Asia/Shanghai（界面可显示 CST） |
| A 股最小交易单位 | 100 股；碎股平仓允许一次性全额申报 |
| 价格偏离上限 | 2.00% |
| 单票仓位上限 | 20.00% |
| 总杠杆上限 | 2.00x |
| 当日亏损上限 | 3.00% |
| 最大回撤暂停上限 | 8.00% |
| 行情延迟门禁 | 90 秒 |
| 订单超时 | 60 秒 |
| 模拟信用参数 | 默认未启用 |

## 3. 用户、角色与权限边界

0.0.1 仅定义单机个人用户，不实现登录和多用户授权。用户可以执行以下角色动作：

| 角色 | 目标 | 允许动作 |
| --- | --- | --- |
| 研究者 | 创建和验证策略、运行回测、查看结果 | 策略实验室、回测中心、数据中心 |
| 盘中操作员 | 查看信号、委托和风险、暂停模拟 | 总览、模拟执行、账户与风险 |
| 系统维护者 | 修改规则、费用、通知和运行参数 | 设置、数据中心、运行记录 |

同一用户可以承担全部角色。危险操作始终需要输入型确认，不因角色不同而绕过确认。

## 4. 端到端业务流程

### 4.1 策略到模拟

```text
创建草稿 → 配置股票池/周期/数据/因子/模型
       → 数据泄漏检查 → 样本外评估 → 可回测
       → 回测运行 → 结果审阅 → 可模拟
       → 数据门禁检查 → 创建模拟运行 → 信号
       → 风控判定 → 委托 → 成交 → 记账 → 审计
```

任何数据缺口、泄漏检查失败或样本外评估失败都不得进入分钟级模拟。

### 4.2 数据补数

```text
发现延迟/缺口 → 打开诊断 → 同步缺失数据 → 连续性/合法性校验
             → 创建新数据版本 → 标记当前版本 → 新运行引用新版本
```

补数不得覆盖历史版本；历史运行继续引用其原数据版本。

### 4.3 危险操作

```text
点击危险操作 → 显示影响范围/预计影响/生效时间
             → 输入指定确认文本 → 校验通过 → 创建操作运行 ID
             → 更新状态并写入审计事件
```

## 5. 功能需求

### 5.1 全局 Shell 与导航

| ID | 需求 | 验收条件 |
| --- | --- | --- |
| SRS-FR-SHELL-001 | 系统提供固定左侧导航和顶部状态栏 | 8 个页面均可从导航到达，当前页有选中态 |
| SRS-FR-SHELL-002 | 顶部显示交易日、行情时间、数据新鲜度、模拟账户、搜索、告警数和运行状态 | 状态文案包含具体时间/数量/延迟，不使用空泛“正常” |
| SRS-FR-SHELL-003 | 点击状态项打开诊断抽屉或带筛选的详情页 | 从状态到详情最多 1 次点击 |
| SRS-FR-SHELL-004 | 全局搜索支持策略、标的和运行 ID 的关键词输入 | 回车后显示搜索反馈；无关键词时提示下一步 |
| SRS-FR-SHELL-005 | 风险告警数量大于 0 时显示计数徽章 | 点击进入账户与风险并保留事件筛选 |

### 5.2 总览

| ID | 需求 |
| --- | --- |
| SRS-FR-OV-001 | 展示数据源、交易日历、模拟时钟、任务队列、数据库和最近错误六项健康信息。 |
| SRS-FR-OV-002 | 展示净资产、可用资金、持仓市值、融资负债、融券负债、总杠杆、维持担保比例和今日收益。 |
| SRS-FR-OV-003 | 展示单票集中度、行业集中度、最大回撤和当日亏损，统一使用“当前值 / 上限”。 |
| SRS-FR-OV-004 | 策略运行表至少包含策略名称、策略版本、数据版本、状态、周期、信号、目标换手、今日 P&L、最近运行和操作。 |
| SRS-FR-OV-005 | 执行队列展示待风控、已提交、部分成交、已完成和已拒绝数量；点击状态进入模拟执行并保留筛选。 |
| SRS-FR-OV-006 | 净值区域支持净值、基准、回撤、杠杆和行业暴露切换，默认近 30 个交易日。 |
| SRS-FR-OV-007 | 审计时间线按倒序展示信号、风控判定、委托、成交、数据异常和人工操作。 |
| SRS-FR-OV-008 | “查看差异”只展示本地账本与账户快照差异，不覆盖任一数据源。 |
| SRS-FR-OV-009 | “运行本次模拟”先显示数据时间、待执行策略、预计委托数和风险检查结果。 |
| SRS-FR-OV-010 | “暂停全部策略”必须使用输入“确认暂停”，暂停后仍允许平仓、撤单和风险处理。 |

### 5.3 策略实验室

| ID | 需求 |
| --- | --- |
| SRS-FR-LAB-001 | 页面采用三栏结构：策略/实验列表、参数与数据配置、信号/净值/因子分析。 |
| SRS-FR-LAB-002 | 策略列表展示名称、收藏状态、生命周期、最近收益、最近实验 ID 和时间。 |
| SRS-FR-LAB-003 | 配置必须包含股票池、运行周期、数据范围、复权方式、因子/特征、模型版本、参数、单票仓位上限和交易成本模型。 |
| SRS-FR-LAB-004 | 数据泄漏检查失败时阻断策略验证，并显示错误原因和运行 ID。 |
| SRS-FR-LAB-005 | 样本外评估只有通过后才能将策略标记为可模拟。 |
| SRS-FR-LAB-006 | 分钟数据延迟超过 90 秒或存在缺口时，阻断分钟级模拟但允许保存研究草稿。 |
| SRS-FR-LAB-007 | 分析区域支持净值、信号、因子和错误 Tab；错误视图提供运行记录入口。 |
| SRS-FR-LAB-008 | 支持保存草稿、验证策略、复制策略和创建实验；复制创建新草稿，不继承旧运行记录。 |

### 5.4 回测中心

| ID | 需求 |
| --- | --- |
| SRS-FR-BT-001 | 回测配置为单页配置，不使用多步骤向导。 |
| SRS-FR-BT-002 | 配置必须包含策略版本、数据版本、周期、起止日期、初始资金、信用参数、费用、滑点、成交假设、基准和随机种子。 |
| SRS-FR-BT-003 | 所有配置写入运行快照，运行 ID 可从结果页和运行记录访问。 |
| SRS-FR-BT-004 | 运行摘要展示状态、阶段、进度、耗时、数据泄漏检查、样本外范围和成本模型。 |
| SRS-FR-BT-005 | 结果至少展示年化收益、波动率、夏普、最大回撤、胜率、换手、交易成本占比、融资利息、融券费用和强平次数。 |
| SRS-FR-BT-006 | 每个指标可打开贡献明细；交易明细至少包含时间、标的、方向、数量、价格和净贡献。 |
| SRS-FR-BT-007 | 对比最多选择 4 个实验；日期范围和成本模型固定；数据版本不同不得混绘并必须显示警告。 |

### 5.5 模拟执行

| ID | 需求 |
| --- | --- |
| SRS-FR-SIM-001 | 顶部显示模拟时钟、行情时间、当前批次、数据质量、待风控数量和部分成交数量。 |
| SRS-FR-SIM-002 | 独立策略信号流必须包含策略、标的、信号原因、目标仓位、生成时间和数据快照链接。 |
| SRS-FR-SIM-003 | 信号快照链接必须关联策略版本、数据版本和运行 ID，并可打开完整审计链路。 |
| SRS-FR-SIM-004 | 委托表必须包含标的、策略、方向、委托数量、剩余数量、价格、状态和时间。 |
| SRS-FR-SIM-005 | 支持按策略、标的、状态和买卖方向组合筛选；无结果时提供调整筛选或前往数据中心的下一步。 |
| SRS-FR-SIM-006 | 风险栏显示集中度、总杠杆、当日亏损、保证金、可卖数量、涨跌停检查、融资/融券额度和最近拒绝原因。 |
| SRS-FR-SIM-007 | 恢复执行前必须检查行情延迟门禁和数据缺口；门禁未通过时保持暂停并说明修复动作。 |
| SRS-FR-SIM-008 | 时间线固定串联信号、风控判定、委托、成交和记账五类事件。 |

### 5.6 账户与风险

| ID | 需求 |
| --- | --- |
| SRS-FR-RISK-001 | 资产摘要展示现金、总持仓、融资、融券、利息、净资产和维持担保比例。 |
| SRS-FR-RISK-002 | 持仓表必须区分总持仓、可用持仓和 T+1 冻结，并展示成本价、现价、浮盈亏、策略归属、行业和边际风险贡献。 |
| SRS-FR-RISK-003 | 持仓市值必须提供闭合说明，无法逐标的拆分时以“其他持仓”汇总并说明金额。 |
| SRS-FR-RISK-004 | 限额面板至少包含单票、行业、总杠杆、单日亏损和回撤暂停，并显示使用率与当前值/上限。 |
| SRS-FR-RISK-005 | 风险限额编辑保存前显示影响范围、预计行为和生效时间；保存创建新的配置版本。 |
| SRS-FR-RISK-006 | 风险事件支持预警、阻断、模拟强平和已解决状态，并可按运行 ID审计。 |
| SRS-FR-RISK-007 | 模拟强平必须输入“确认模拟强平”，确认后仅创建模拟运行，不发送真实委托。 |

### 5.7 数据中心

| ID | 需求 |
| --- | --- |
| SRS-FR-DATA-001 | 数据集表展示数据集、类型、来源、覆盖区间、最后更新时间、缺失率、缺失桶数、复权状态、许可证/限制和状态。 |
| SRS-FR-DATA-002 | 所有时间显示 Asia/Shanghai 或 CST；表头明确时区。 |
| SRS-FR-DATA-003 | 质量检查至少包含时间连续性、OHLC 合法性、涨跌停价格和复权因子跳变。 |
| SRS-FR-DATA-004 | 同步任务显示阶段、进度、耗时、取消按钮、日志入口和新版本 ID。 |
| SRS-FR-DATA-005 | 补数成功后创建新版本并将其标记为当前版本，旧版本保持可读、可引用。 |
| SRS-FR-DATA-006 | 版本历史必须包含版本 ID、创建时间、变更摘要、质量结果和是否当前版本。 |
| SRS-FR-DATA-007 | 数据延迟或缺口必须展示具体分钟数/缺失桶数、影响范围和诊断入口。 |

### 5.8 运行记录

| ID | 需求 |
| --- | --- |
| SRS-FR-RUN-001 | 汇总展示今日运行、运行中、已完成、警告、失败和最长排队。 |
| SRS-FR-RUN-002 | 任务表展示运行 ID、任务、策略、状态、开始时间、耗时和操作。 |
| SRS-FR-RUN-003 | 支持全部、运行中、警告和失败筛选，并支持 URL 状态参数。 |
| SRS-FR-RUN-004 | 失败详情必须展示错误码、人话说明、影响范围和运行 ID。 |
| SRS-FR-RUN-005 | 失败任务提供可复制 CLI 重跑命令；重跑创建新运行，不修改旧记录。 |
| SRS-FR-RUN-006 | 审计事件支持信号、风控、委托、成交、记账、数据异常和人工操作类型。 |

### 5.9 设置

| ID | 需求 |
| --- | --- |
| SRS-FR-SET-001 | 交易规则配置市场、最小交易单位、价格偏离上限、当日亏损上限、单票仓位上限和总杠杆上限。 |
| SRS-FR-SET-002 | 交易规则配置 T+1 可用数量约束和涨跌停价格校验开关。 |
| SRS-FR-SET-003 | 费用模型配置佣金、最低佣金、卖出印花税、沪市过户费、默认滑点、成交假设和价格取整。 |
| SRS-FR-SET-004 | 模拟默认值配置初始资金、订单超时、行情延迟阈值和随机种子。 |
| SRS-FR-SET-005 | 配置数据异常时暂停新信号、允许平仓和撤单继续执行。 |
| SRS-FR-SET-006 | 通知配置风险阈值、策略失败、数据延迟/缺口和任务摘要。 |
| SRS-FR-SET-007 | 保存新版本显示配置版本、影响范围、旧值、新值和生效时间；撤销恢复上一版本但不改写历史运行快照。 |

## 6. 业务规则与风控规则

### 6.1 事前风控

| 规则 ID | 判定 | 处理 |
| --- | --- | --- |
| SRS-BR-RISK-001 | 单票拟持仓市值 / 总资产 > 20% | 阻断买单，允许卖单和平仓 |
| SRS-BR-RISK-002 | 委托数量未按 100 股对齐 | 买入数量向下取整；碎股平仓必须一次性全额申报 |
| SRS-BR-RISK-003 | 单秒请求 > 5 笔或单分钟 > 30 笔 | 进入延迟队列，记录流控事件 |
| SRS-BR-RISK-004 | 存在同标的未成交冲突挂单 | 先撤后挂，记录自成交防护事件 |
| SRS-BR-RISK-005 | 委托价偏离最新价 > 2% | 拒绝委托，记录价格偏离原因 |
| SRS-BR-RISK-006 | 行情延迟 > 90 秒或关键分钟桶缺失 | 暂停新信号和新开仓；平仓、撤单和风险处理可继续 |

### 6.2 T+1 持仓

```text
Total_Shares = Available_Shares + Frozen_Shares
```

- T 日买入成功：总持仓增加，冻结数量增加，可用数量不变；
- T+1 开盘前：可用数量等于总持仓，冻结数量归零；
- 所有持仓表和模拟风险栏必须使用同一账户快照时间。

### 6.3 特殊交易时段

- 09:15–09:25 为集合竞价；09:20–09:25 禁止撤单；
- 涨停且卖一量为 0 时禁止生成买单；
- 跌停或价格异常时，卖出动作进入规则检查并记录原因；
- 以上规则在本版本只作用于模拟执行。

## 7. 状态模型

### 7.1 策略状态

```text
草稿 → 待验证 → 可回测 → 可模拟 → 已暂停
  └──────────────→ 错误
```

状态转换约束：

- 草稿可保存、复制和创建实验；
- 待验证必须完成数据泄漏检查；
- 可回测必须有可用数据版本和完整参数；
- 可模拟必须通过样本外评估和数据门禁；
- 已暂停可恢复，但恢复前必须重新检查数据门禁；
- 错误必须有错误码和运行记录入口，不能直接进入可模拟。

### 7.2 任务状态

统一支持：`pending`、`running`、`completed`、`canceled`、`error`。

每个 running 任务必须保存阶段、百分比、开始时间、已耗时、取消能力和日志入口。error 必须保存错误码、描述和运行 ID。

### 7.3 委托状态

统一支持：待风控、已提交、部分成交、已完成、已拒绝、已撤销。

部分成交必须同时保存已成交数量和剩余数量；已拒绝必须关联风控检查项。

### 7.4 风险事件状态

统一支持：预警、阻断、模拟强平、已解决。状态变化必须写入审计事件，不得静默覆盖。

## 8. 数据契约

### 8.1 通用实体

| 实体 | 必填字段 |
| --- | --- |
| Strategy | strategy_id、name、version、status、parameters、data_version、created_at、updated_at |
| Experiment | experiment_id、strategy_version、data_version、cost_model、date_range、status、run_id |
| Run | run_id、run_type、status、stage、progress、started_at、ended_at、error_code |
| DatasetVersion | dataset_id、version_id、source、coverage_start、coverage_end、created_at、quality_result、is_current |
| Signal | signal_id、run_id、strategy_id、symbol、reason、target_weight、generated_at、data_snapshot |
| Order | order_id、run_id、strategy_id、symbol、side、quantity、remaining_quantity、price、status、created_at |
| PositionSnapshot | snapshot_at、symbol、total_shares、available_shares、frozen_shares、avg_cost、last_price |
| RiskEvent | event_id、run_id、event_type、severity、rule_id、message、status、created_at |
| AuditEvent | event_id、run_id、event_type、occurred_at、summary、payload |
| ConfigVersion | config_version_id、changed_fields、old_values、new_values、effective_at、created_at |

### 8.2 版本和可复现性

任何收益、风险、交易明细和审计事件必须可以反查：

```text
结果 → run_id → strategy_version + data_version + cost_model + random_seed
```

缺少任一关键版本字段的结果不得标记为可复现。

### 8.3 时间和金额

- 持久化时间使用带时区的时间戳，界面统一 Asia/Shanghai；
- 金额使用人民币，保留 2 位小数；
- 价格保留 2 位小数；
- 数量为整数股；
- 百分比保留 2 位；
- 所有运行 ID 和版本 ID 使用等宽字体并支持复制。

## 9. 交互与错误处理

### 9.1 表格交互

所有业务表格必须支持：关键列固定、排序三态、列显隐、单元格复制、CSV 导出、键盘导航、行选择、筛选和空结果提示。

### 9.2 抽屉

抽屉用于诊断、运行链路、版本历史、差异详情和确认预览；打开不阻塞主工作区滚动，关闭后焦点返回触发控件。

### 9.3 错误文案

错误必须包含：错误码、用户可理解的说明、影响范围、关联运行 ID和下一步动作。适用时提供“打开运行记录”和“复制 CLI 重跑命令”。

### 9.4 空数据

空状态必须说明原因并提供下一步，例如“尚无回测实验 — 创建第一个实验”。不得只显示“暂无数据”。

### 9.5 危险确认

确认层必须显示影响范围、预计影响、生效时间和输入确认文本；确认按钮使用动词短语，不使用“确定”或“OK”。

## 10. 非功能需求

| ID | 类别 | 要求 |
| --- | --- | --- |
| SRS-NFR-PERF-001 | 性能 | 桌面 1440px 首屏无横向滚动；常规页面首次可交互目标 ≤ 2 秒（本地演示环境）。 |
| SRS-NFR-PERF-002 | 性能 | 表格筛选、排序、列显隐和抽屉打开应在 200ms 内反馈。 |
| SRS-NFR-RESP-001 | 响应式 | 1180px 保留主表格和风险栏，隐藏次要列；低于 1024px 导航收缩；移动端控件可触达。 |
| SRS-NFR-A11Y-001 | 可访问性 | 文本对比度 ≥ 4.5:1；状态同时使用图标和文案；所有交互可键盘到达。 |
| SRS-NFR-A11Y-002 | 可访问性 | 抽屉/确认层支持焦点管理、Esc 关闭和焦点回归；表格支持键盘导航。 |
| SRS-NFR-SEC-001 | 安全 | 0.0.1 不发送真实委托；所有危险动作只产生模拟运行和审计事件。 |
| SRS-NFR-SEC-002 | 安全 | CLI 重跑命令不得包含密钥、密码或不可公开的凭证。 |
| SRS-NFR-DATA-001 | 数据 | 历史数据版本、运行快照和审计记录不可被补数或重跑覆盖。 |
| SRS-NFR-DATA-002 | 数据 | 任何收益结果必须关联策略、数据、成本和运行版本。 |
| SRS-NFR-MOTION-001 | 体验 | 动效仅用于状态反馈；尊重 `prefers-reduced-motion`。 |
| SRS-NFR-COPY-001 | 文案 | 状态文案具体到数量、时间、影响和下一步，不使用空泛状态词。 |

## 11. 验收标准

### 11.1 功能验收

| ID | 验收条件 |
| --- | --- |
| SRS-AC-001 | 8 个页面入口可加载，导航、顶部状态和抽屉行为一致。 |
| SRS-AC-002 | 总览可在 2 次点击内进入任意成交的五节点审计链路。 |
| SRS-AC-003 | 暂停全部策略和模拟强平的错误确认不会提交，正确确认产生操作运行 ID。 |
| SRS-AC-004 | 分钟数据延迟/缺口会阻断新开仓，但平仓、撤单和风险处理保持可用。 |
| SRS-AC-005 | 回测对比最多 4 个实验，不同数据版本不混绘并显示警告。 |
| SRS-AC-006 | 补数产生新数据版本，版本历史可展示版本 ID、时间、摘要和质量结果。 |
| SRS-AC-007 | 部分成交显示剩余数量，T+1 持仓区分总持仓、可用和冻结。 |
| SRS-AC-008 | 所有收益、风险和成交详情均能追溯到策略版本、数据版本、成本模型和运行 ID。 |

### 11.2 视觉与响应式验收

| ID | 验收条件 |
| --- | --- |
| SRS-AC-009 | 1440×900 首屏无横向滚动；1180px 下主表格和风险栏仍可用。 |
| SRS-AC-010 | 所有表格具备排序、列显隐、复制、CSV 导出、键盘导航和空结果状态。 |
| SRS-AC-011 | 正常、加载、空、延迟/缺口、错误、长任务和危险确认状态均可演示。 |
| SRS-AC-012 | 不出现渐变、装饰性插画、大圆角卡片、纯颜色状态或“确定/OK”危险按钮。 |

### 11.3 可追溯验收

测试用例必须引用本文件的需求 ID，并至少覆盖：

- 每个页面一个正常状态和一个异常/空状态；
- 每类危险操作的错误确认和正确确认；
- 每类任务的 pending/running/completed/canceled/error；
- 每张业务表格的排序、列显隐、复制、导出和响应式折叠；
- 一条完整的信号到记账审计链路；
- 一次补数后的版本历史和旧版本保留。

## 12. 待决策项

以下事项在没有产品确认前，实施人员不得自行扩展：

| ID | 问题 | 当前默认 |
| --- | --- | --- |
| SRS-D-001 | 是否接入 MiniQMT/PTrade | 0.0.1 不接入真实柜台 |
| SRS-D-002 | 是否启用融资融券 | 默认未启用，界面显示不适用或 ¥0.00 |
| SRS-D-003 | 是否实现策略代码编辑器 | 本期不实现，保留参数/因子字段 |
| SRS-D-004 | 数据许可证是否提供外部原文链接 | 本期明文显示限制，链接后续补充 |
| SRS-D-005 | 是否实现多用户权限 | 本期单机单用户 |

## 13. 变更控制

新增字段、改变状态含义、改变危险操作流程或改变页面信息架构时，必须：

1. 更新 Open Design 原型；
2. 更新 `REQUIREMENTS-0.0.1.md`；
3. 更新本文对应的 `SRS-FR-*`、`SRS-NFR-*` 或 `SRS-AC-*`；
4. 补充或修改测试用例；
5. 在变更记录中说明旧行为、新行为、影响范围和迁移方式。

不得只修改原型而不更新规格说明书，也不得只修改规格说明书而保留不一致的原型。

## 14. 变更记录

| 日期 | 版本 | 变更 |
| --- | --- | --- |
| 2026-08-13 | 0.0.1 | 初版规格说明书；固化 8 页面、字段、状态、业务规则、数据契约和验收标准。 |
