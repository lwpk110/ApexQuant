# ApexQuant 0.0.1 UI Design

> 本文是 ApexQuant 0.0.1 的界面设计规范与原型输入稿。
> 目标用户是独立量化交易者，使用个人电脑进行 A 股研究、回测、分钟级模拟执行和风险复盘。
> 本版不连接真实券商，不把免费行情包装成实盘级数据。

## 1. 设计定位

### 1.1 产品气质

ApexQuant 是一个“交易控制室 + 策略实验室”，不是营销型金融 App。界面要让用户在几秒内回答四件事：

1. 系统和数据现在是否健康。
2. 当前账户承担了多少风险。
3. 哪些策略产生了什么信号，哪些委托正在等待成交。
4. 今天发生的每一笔交易是否可以解释和追溯。

### 1.2 视觉方向

- 深色工作台，背景使用近黑的中性蓝灰，不使用大面积渐变。
- 数据区域使用高对比浅色文字，数值采用等宽数字字体。
- 绿色只表达盈利/健康，红色只表达亏损/风险，琥珀色表达等待或需确认，蓝色表达选中和数据来源。
- 采用细边框、分隔线和紧凑表格建立层级；避免“卡片套卡片”和装饰性圆角。
- 重要操作使用图标 + 动词；危险操作必须有二次确认和影响范围。

### 1.3 桌面基准

- 优先支持 1440 x 900 和 1920 x 1080。
- 最小可用宽度 1180px；低于该宽度时保留主表格和风险栏，次要图表折叠到抽屉。
- 左侧导航固定 224px；顶部状态栏固定 52px；主工作区允许独立滚动。
- 颜色、间距、表格列宽必须使用设计 token，避免页面间出现不同的视觉规则。

### 1.4 Design tokens

| Token | 值 | 用途 |
| --- | --- | --- |
| `bg.canvas` | `#0B1017` | 应用背景 |
| `bg.surface` | `#111923` | 工作区和侧栏 |
| `bg.elevated` | `#17222E` | 抽屉、菜单、确认层 |
| `border.default` | `#263646` | 表格和区域分隔 |
| `text.primary` | `#E8EEF5` | 主文本 |
| `text.secondary` | `#91A1B2` | 辅助文本、时间戳 |
| `accent.blue` | `#36A3FF` | 选中、链接、数据来源 |
| `state.positive` | `#39C58A` | 盈利、健康、通过 |
| `state.warning` | `#E5A93D` | 等待、延迟、需确认 |
| `state.negative` | `#F0646A` | 亏损、阻断、错误 |

字体使用系统无衬线字体；行情数字、金额、时间和运行 ID 使用等宽字体。基础字号 13px，页面标题 20px，区域标题 14px，辅助信息 12px。间距基数为 4px，常用区域间距为 16px 和 24px；卡片圆角不超过 6px。

## 2. 应用 Shell

```text
+----------------------+-----------------------------------------------+
| ApexQuant             | 交易日  数据时钟  模拟账户  全局搜索  告警  用户 |
|                      +-----------------------------------------------+
| 总览                 |                                               |
| 策略实验室           |                 主工作区                     |
| 回测中心             |                                               |
| 模拟执行             |                                               |
| 账户与风险           |                                               |
| 数据中心             |                                               |
| 运行记录             |                                               |
| 设置                 |                                               |
|                      |                                               |
| 数据源状态           |                                               |
+----------------------+-----------------------------------------------+
```

### 2.1 左侧导航

导航项显示图标、名称和未读数量。当前页用细窄蓝色指示条与浅色底，不使用大面积高亮。

- 总览：账户、策略、订单、风险和系统健康的统一入口。
- 策略实验室：策略模板、因子、特征、参数和实验结果。
- 回测中心：数据范围、执行模型、指标、交易明细和对比。
- 模拟执行：盘中时钟、信号、委托、成交和暂停/恢复。
- 账户与风险：资金、持仓、融资融券、限额、预警和模拟强平。
- 数据中心：数据源、质量检查、缓存、补数和版本。
- 运行记录：任务、日志、错误、审计事件和重跑入口。
- 设置：交易规则、费用、滑点、策略默认参数和通知渠道。

### 2.2 顶部状态栏

顶部状态栏永远显示：交易日状态、最近行情时间、数据新鲜度、模拟账户名称、全局搜索、风险告警数量和运行状态。

状态文案必须具体，例如“分钟数据延迟 2 分钟”而不是“连接正常”。点击状态可打开对应的诊断抽屉。

## 3. 总览页：Command Center

总览页服务于盘中快速判断，不承载复杂配置。布局按“健康 -> 风险 -> 执行 -> 研究”排序。

### 3.1 首屏区域

1. **系统健康条**：数据源、交易日历、模拟时钟、任务队列、数据库和最近错误。每项有状态、时间戳和诊断入口。
2. **账户摘要**：净资产、可用资金、持仓市值、融资负债、融券负债、总杠杆、维持担保比例、今日收益。
3. **风险摘要**：最大回撤、单票集中度、行业集中度、当日亏损限额、待处理风险事件。风险数字旁必须显示“当前值 / 上限”。
4. **策略运行表**：策略名、周期、状态、信号数、目标换手、今日 P&L、最近运行时间、数据版本、暂停/恢复操作。
5. **执行队列**：待风控、已提交、部分成交、已完成、拒绝的委托数量；点击进入模拟执行页并保留筛选条件。
6. **净值与暴露图**：净值、基准、回撤、杠杆、行业暴露可切换；默认显示近 30 个交易日。
7. **审计时间线**：按时间倒序显示信号、风控判定、委托、成交、数据异常和人工操作。

### 3.2 总览页关键操作

- `运行本次模拟`：打开确认条，列出数据时间、待执行策略、预计委托数和风险检查状态。
- `暂停全部策略`：必须二次确认；暂停后仍允许平仓和风险处理。
- `查看差异`：展示本地账本与最近账户快照的差异，不直接覆盖数据。
- `打开运行记录`：带着当前运行 ID进入可重放的审计视图。

## 4. 策略实验室

策略实验室采用三栏工作区，减少页面跳转：

```text
策略/实验列表 | 参数与数据配置 | 信号、净值和因子分析
```

- 左栏：策略分组、标签、运行状态、最近收益、最近实验；支持收藏和复制。
- 中栏：股票池、周期、数据范围、复权方式、因子/特征、模型、交易成本和风险约束。
- 右栏：信号表、组合权重、净值曲线、回撤、换手、行业暴露、特征重要性和错误提示。

策略状态分为 `草稿`、`待验证`、`可回测`、`可模拟`、`已暂停`、`错误`。只有通过数据泄漏检查和样本外评估的策略才能标记为“可模拟”。

## 5. 回测中心

回测创建流程使用单页配置 + 右侧运行摘要，不使用多步骤向导。

必须可见的配置：策略版本、数据版本、周期、起止日期、初始资金、信用参数、费用、滑点、成交假设、基准和随机种子。

结果页优先展示可解释信息：年化收益、波动率、夏普、最大回撤、胜率、换手、交易成本占比、融资利息、融券费用和强平次数。每个指标都能展开到交易明细。

对比模式支持最多 4 个实验并排比较，固定显示相同的日期范围和成本模型；不允许把不同数据版本的结果混在一张图里而不提示。

## 6. 模拟执行页

模拟执行页是盘中主工作台，强调时间顺序和委托状态：

- 顶部：模拟时钟、行情时间、运行/暂停、当前批次和数据质量提示。
- 左侧：策略信号流，显示信号原因、目标仓位、生成时间和数据快照链接。
- 中央：委托与成交表，支持按策略、标的、状态、买卖方向筛选；部分成交必须显示剩余数量。
- 右侧：账户风险、保证金、可卖数量、涨跌停阻断、融资融券可用额度和最近风控拒绝原因。
- 底部：事件时间线，允许展开“信号 -> 风控 -> 委托 -> 成交 -> 记账”的完整链路。

## 7. 账户与风险页

账户页使用“摘要 + 明细”结构，不将关键风险藏在弹窗中：

- 资产摘要：现金、持仓、融资、融券、利息、净资产和保证金率。
- 持仓表：总持仓、可用持仓、冻结、成本、现价、浮盈亏、策略归属、行业和风险贡献。
- 限额编辑：单票、行业、总杠杆、单日亏损、回撤暂停；保存前显示影响范围。
- 风险事件：预警、阻断、强平模拟和已解决事件，支持按运行 ID审计。

## 8. 数据中心与运行记录

数据中心展示每个数据集的来源、最后更新时间、覆盖区间、缺失率、复权状态和许可证/使用限制。补数操作产生新数据版本，不覆盖旧版本。

运行记录按任务、运行 ID和严重级别组织。任何失败都提供可复制的 CLI 重跑命令；重跑默认创建新运行，不修改旧审计记录。

## 9. 交互与状态规范

- 所有长任务显示阶段、进度、已耗时、可取消状态和日志入口。
- 空状态必须告诉用户下一步，例如“尚无回测实验，创建第一个实验”。
- 数据过期、数据缺口、策略错误、风控阻断、模拟暂停使用不同颜色和图标，不能只靠颜色区分。
- 表格支持固定关键列、列显隐、排序、复制单元格和导出；金额、数量、价格和比例使用明确的小数位规则。
- 危险操作使用明确动词，如“暂停全部策略”“模拟强平”，不使用模糊的“确定”。
- 命令行与 Web 操作共享运行 ID，CLI 输出可直接跳转到 Web 详情页。

## 10. CLI 对应工作流

```text
apexquant doctor                 检查数据源、数据库、交易日历和配置
apexquant data sync              同步并校验指定数据集
apexquant backtest run           创建可复现回测
apexquant backtest compare       对比实验
apexquant sim start              启动日频或分钟级模拟
apexquant sim pause              暂停策略执行
apexquant sim reconcile          生成本地账本差异报告
apexquant run show <run-id>      查看运行与审计事件
```

CLI 默认输出适合终端阅读的摘要，并提供 `--json` 供脚本和 Web 服务调用。

## 11. 原型验收标准

原型必须至少覆盖总览、策略实验室、回测结果、模拟执行、账户风险和数据诊断六个页面，并展示以下状态：

- 正常运行、数据延迟、数据缺口、策略错误、风控阻断、部分成交、模拟暂停、模拟强平预警。
- 1440px 桌面宽度下首屏无需横向滚动；表格在窄窗口能够折叠次要列。
- 用户从总览进入一笔成交的审计链路不超过 2 次点击。
- 用户能在不离开当前页面的情况下暂停全部策略并看到生效时间。
- 任何收益数字都能追溯到策略版本、数据版本、成本模型和运行 ID。

## 12. Open Design 原型输入

请以“专业个人量化交易控制室”为主题生成桌面端高保真原型：深色中性蓝灰背景，青蓝色数据强调，绿色盈利，红色风险，琥珀色待处理；使用紧凑表格、等宽数字、细边框和明确分隔线。首屏优先呈现系统健康、账户风险、策略状态、执行队列和净值图，不使用营销型 Hero、装饰性渐变、大面积圆角卡片或纯展示型插画。页面必须包含加载、空数据、数据延迟、错误、暂停和确认状态。
