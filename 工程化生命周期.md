
# ApexQuant 工程化生命周期

## 需求进入

需求先写入 `REQUIREMENTS-0.0.1.md`，形成字段、用户故事、状态、验收和追踪矩阵；正式实现契约写入 `SRS-0.0.1.md`。当字段、状态或危险操作改变时，先同步 Open Design 原型，再修改文档基线。

## 设计与拆解

设计 token、交互和可访问性以 `DESIGN.md` 为准，页面信息架构以 `UI-DESIGN.md` 为准。实现任务必须能映射到需求 ID，禁止只以“页面完成”作为验收。

## 开发

代码按 `domain -> application -> adapters/interfaces` 分层。领域规则保持无副作用和可单元测试；所有运行、版本和审计数据必须可追溯。0.0.1 的真实券商接入明确关闭。

## 验证

每次变更至少运行单元测试、Python 编译检查和文档/格式检查。跨模块变更追加状态矩阵、错误路径、空数据和危险确认验收；UI 变更追加 1440px、1180px 和移动端检查。

## 发布

发布前核对需求追踪矩阵、迁移脚本、配置默认值和运行手册。运行快照必须包含策略版本、数据版本、成本模型和随机种子；历史运行只读，不被新版本覆盖。
