# ApexQuant 设计规范（DESIGN.md）

> **读者**：Design / Frontend Agent，以及任何参与 ApexQuant 前端实现的工程师。
> **作用**：定义产品的视觉身份、设计 Token、组件渲染规则与状态反馈规范，确保任何 Agent 在任何页面上产出的 UI 都一致、可验收。
> **上游依据**：产品定位与页面结构见 [UI-DESIGN.md](./UI-DESIGN.md)；交易规则与业务语义见 [PRD-0.0.1.md](./PRD-0.0.1.md)。本文档与上游冲突时，以上游为准，本文档只补充「怎么渲染」。

---

## 1. 视觉身份（Visual Identity）

### 1.1 一句话气质

**交易控制室 + 策略实验室**：冷静、密集、可审计的专业工作台，不是营销型金融 App。

### 1.2 用户打开界面后 5 秒内必须能回答

1. 系统和数据现在是否健康；
2. 当前账户承担了多少风险；
3. 哪些策略产生了信号、哪些委托在等待成交；
4. 今天每一笔交易是否可以解释和追溯。

任何装饰性元素如果妨碍以上四点，一律删除。

### 1.3 美学关键词

深色工作台 / 高密度信息 / 细边框分隔 / 等宽数字 / 语义化色彩 / 零插画零渐变。

### 1.4 明确禁止

- 营销型 Hero 区、Banner 大图、装饰性插画、表情符号图标。
- 大面积渐变、玻璃拟态（backdrop-blur）、发光阴影、霓虹色。
- 大圆角（> 6px）卡片、卡片套卡片超过两层。
- 用红/绿/琥珀做纯装饰；三色只允许表达语义。
- 仅用颜色区分状态——所有状态必须同时有图标 + 文案。
- 空按钮文案「确定 / OK / 提交」——操作按钮必须是动词短语。

---

## 2. 设计 Token

实现时**必须**通过 CSS 自定义属性引用，禁止在组件里硬编码色值与字号。

### 2.1 颜色

```css
:root {
  /* 背景层级：canvas < surface < elevated，逐级抬升 */
  --bg-canvas:      #0B1017;  /* 应用背景 */
  --bg-surface:     #111923;  /* 工作区、侧栏、表格底 */
  --bg-elevated:    #17222E;  /* 抽屉、菜单、确认层、悬浮层 */
  --bg-hover:       #1B2836;  /* 行悬停、按钮悬停 */
  --bg-selected:    #14304A;  /* 选中行/导航项底色（低饱和蓝） */

  /* 边框与分隔 */
  --border-default: #263646;  /* 表格线、区域分隔，统一 1px */
  --border-strong:  #35485C;  /* 悬浮层边缘、聚焦输入框 */

  /* 文本 */
  --text-primary:   #E8EEF5;  /* 主文本 */
  --text-secondary: #91A1B2;  /* 辅助文本、时间戳、标签 */
  --text-disabled:  #556575;  /* 禁用 */

  /* 强调与语义色：只允许表达语义 */
  --accent-blue:    #36A3FF;  /* 选中、链接、数据来源标记 */
  --state-positive: #39C58A;  /* 盈利、健康、风控通过 */
  --state-warning:  #E5A93D;  /* 等待、数据延迟、需确认 */
  --state-negative: #F0646A;  /* 亏损、阻断、错误、拒绝 */

  /* 语义色的容器底（徽章、横幅用，透明度 12%） */
  --positive-bg: rgb(57 197 138 / 0.12);
  --warning-bg:  rgb(229 169 61 / 0.12);
  --negative-bg: rgb(240 100 106 / 0.12);
  --accent-bg:   rgb(54 163 255 / 0.12);
}
```

### 2.2 字体排印

```css
:root {
  --font-ui:   -apple-system, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif;
  --font-mono: "JetBrains Mono", "SF Mono", Consolas, "Courier New", monospace;

  --text-page-title: 20px;  /* 页面标题，字重 600 */
  --text-section:    14px;  /* 区域标题，字重 600 */
  --text-body:       13px;  /* 正文、表格，字重 400 */
  --text-caption:    12px;  /* 辅助信息、时间戳，--text-secondary */
}
```

数字规则（强制）：

- 金额、价格、数量、比例、时间、运行 ID 一律使用 `var(--font-mono)` + `font-variant-numeric: tabular-nums`，数值列右对齐。
- 小数位：价格 2 位；金额千分位加 `¥` 前缀；比例 2 位百分比；股数整数并标注「股」。
- 时间格式 `YYYY-MM-DD HH:mm:ss`（Asia/Shanghai）；表格内可省略日期为 `HH:mm:ss`。

### 2.3 间距、圆角、阴影

```css
:root {
  --space-1: 4px;   /* 基数，所有间距为其倍数 */
  --space-2: 8px;   /* 组件内部紧凑间距 */
  --space-3: 12px;  /* 组件内距 */
  --space-4: 16px;  /* 区域间距 */
  --space-6: 24px;  /* 大区块间距 */

  --radius-sm: 4px; /* 按钮、输入框、徽章 */
  --radius-md: 6px; /* 面板、表格容器——全站圆角上限 */

  --shadow-overlay: 0 8px 24px rgb(0 0 0 / 0.45); /* 仅悬浮层使用，禁止彩色/发光阴影 */
}
```

---

## 3. 布局骨架（App Shell）

```text
┌──────────── 224px ───────────┬────────────────────────────────┐
│ 品牌区 ApexQuant              │ 顶部状态栏（固定 52px）          │
├──────────────────────────────┼────────────────────────────────┤
│ 左侧导航                      │                                │
│ （8 项，图标+名称+未读数）     │      主工作区（独立滚动）        │
│                              │                                │
│ 底部：数据源状态摘要           │                                │
└──────────────────────────────┴────────────────────────────────┘
```

- 左侧导航固定 `224px`；顶部状态栏固定 `52px`；主工作区 `overflow-y: auto` 独立滚动。
- 桌面基准 1440×900 与 1920×1080；**最小可用宽度 1180px**。低于 1180px 时：保留主表格与风险栏，次要图表折叠进右侧抽屉，禁止横向整页滚动。
- 当前导航项：左侧 `2px var(--accent-blue)` 指示条 + `var(--bg-selected)` 底色，不做大面积高亮。

### 3.1 顶部状态栏（常驻，所有页面共享）

从左到右：交易日状态 → 最近行情时间 → 数据新鲜度 → 模拟账户名 → 全局搜索 → 风险告警数 → 运行状态。

渲染规则：

- 状态文案必须具体：写「分钟数据延迟 2 分钟」，禁止写「连接正常 / 异常」这类空话。
- 数据质量用状态点：正常绿点、延迟琥珀点、缺口/中断红点；每个状态可点击，点击打开对应诊断抽屉。
- 风险告警数 > 0 时显示红色计数徽章，点击进入账户与风险页并带上事件筛选。

---

## 4. 组件渲染规范

### 4.1 按钮

| 类型 | 背景 | 文字 | 用途 |
| --- | --- | --- | --- |
| Primary | `var(--accent-blue)` | `#0B1017` | 每屏最多一个主操作（如「运行本次模拟」） |
| Default | 透明 + 1px `var(--border-strong)` | `var(--text-primary)` | 常规操作 |
| Ghost | 透明，悬停 `var(--bg-hover)` | `var(--text-secondary)` | 表格行内操作、工具栏图标按钮 |
| Danger | 透明 + 1px `var(--state-negative)`，悬停 `var(--negative-bg)` | `var(--state-negative)` | 危险操作入口（「暂停全部策略」「模拟强平」） |

- 高度：默认 32px，紧凑表格内 24px；圆角 `var(--radius-sm)`；内边距 `0 12px`。
- 重要操作 = 图标 + 动词短语；加载中按钮显示 spinner 并禁用，保留原文案宽度不抖动。

### 4.2 表格（全站最核心的组件）

```text
容器：var(--bg-surface)，1px var(--border-default)，圆角 6px
表头：32px，12px 字，var(--text-secondary)，底部 1px 分隔线，吸顶
行：  36px，行间 1px var(--border-default)，悬停 var(--bg-hover)
单元格：内边距 0 12px；文本左对齐；数值右对齐等宽
```

能力要求（前端 Agent 实现表格时必须内置，不允许后续补）：

- 固定关键列（横向滚动时名称/代码列钉住）；
- 列显隐、点击表头排序（升/降/取消三态箭头）；
- 单元格单击复制、工具栏导出 CSV；
- 窄窗口下按列优先级折叠次要列，而不是溢出或截断数字。

### 4.3 状态徽章（Status Badge）

结构统一为「状态点 + 文字」，高度 20px，圆角 `var(--radius-sm)`，容器底使用 12% 透明语义色：

| 徽章 | 颜色 | 文案示例 |
| --- | --- | --- |
| 运行中 | positive | 运行中、健康、风控通过 |
| 待处理 | warning | 待验证、部分成交、数据延迟、模拟暂停 |
| 异常 | negative | 错误、已拒绝、风控阻断、数据缺口 |
| 中性 | accent / secondary | 草稿、可回测、可模拟、已完成 |

策略生命周期徽章固定六态：`草稿` `待验证` `可回测` `可模拟` `已暂停` `错误`。「可模拟」必须附带通过数据泄漏检查与样本外评估的标记依据（悬停提示）。

### 4.4 指标条（Metric Strip）

账户/风险摘要使用横向指标条，而非大数字卡片墙：

```text
[ 标签 12px secondary ]
[ 数值 18px mono primary ] [ 环比小字 12px ]
```

- 盈亏类数值带符号并按语义着色（`+¥12,400.00` 绿 / `-¥3,210.50` 红）。
- **风险类指标必须渲染为「当前值 / 上限」**（如 `18.6% / 20.0%`），达到 90% 阈值转琥珀，越限转红并加阻断图标。
- 禁用环形图、仪表盘等装饰性可视化。

### 4.5 图表

- 趋势用折线（净值、回撤），暴露用横向堆叠条，成交分布可用柱状；禁止 3D、饼图、渐变填充面积图。
- 折线主色 `var(--accent-blue)`，基准线 `var(--text-secondary)` 虚线，回撤区域用 12% 透明红。
- 坐标轴与时间轴标签 12px secondary；网格线 `var(--border-default)` 仅横向虚线。
- 图例是可切换开关（净值 / 基准 / 回撤 / 杠杆 / 行业暴露），默认近 30 个交易日。

### 4.6 抽屉与确认层

- 抽屉（诊断、版本历史、差异详情）：右侧滑出，宽 400–480px，`var(--bg-elevated)` + 左侧 1px 边框 + `--shadow-overlay`；打开不阻塞主工作区滚动。
- 危险操作确认层：底部确认条或居中确认层，必须包含四要素——**影响范围、预计影响、生效时间、输入型确认**（输入策略名或「确认暂停」后方可点击）。
- 确认按钮文案 = 动词短语（「暂停全部策略」），禁止「确定 / OK」。

### 4.7 审计时间线（Audit Timeline）

- 按时间倒序，节点类型固定：`信号 → 风控判定 → 委托 → 成交 → 记账`，外加 `数据异常` 与 `人工操作`。
- 每个节点：类型图标 + 时间戳（mono）+ 摘要 + 关联运行 ID；点击展开完整链路视图（同一笔交易的五个节点串联高亮）。
- 验收硬指标：从总览进入任意一笔成交的完整审计链路 ≤ 2 次点击。

### 4.8 空、加载与错误

| 状态 | 渲染规则 |
| --- | --- |
| 空数据 | 线性图标 + 一句说明 + 下一步按钮，如「尚无回测实验 — 创建第一个实验」；禁止只写「暂无数据」 |
| 加载 | 按表格/图表实际结构的骨架屏（shimmer，surface → elevated 呼吸）；禁止整屏 spinner |
| 错误 | 错误码 + 人话描述 + 「打开运行记录」链接；带运行 ID 的错误提供「复制 CLI 重跑命令」按钮 |
| 长任务 | 显示阶段、进度条、已耗时、可取消按钮、日志入口 |

---

## 5. 动效

- 仅用于状态反馈：悬停 100ms、抽屉/确认层滑入 150ms ease-out、骨架屏 shimmer 1200ms 循环。
- 数字刷新不做滚动/翻牌动画；状态切换直接替换，用颜色与图标表达变化。
- 尊重 `prefers-reduced-motion`：开启时所有动画降级为即时替换。

---

## 6. 页面应用要点（规范到页面的映射）

| 页面 | 本规范的高优约束 |
| --- | --- |
| 总览 | 健康条 → 账户摘要 → 风险摘要（当前值/上限）→ 策略表 → 执行队列 → 净值图 → 审计时间线；「暂停全部策略」走 §4.6 四要素确认 |
| 策略实验室 | 三栏工作区不跳页；六态策略徽章（§4.3）；右栏错误面板与运行记录互链 |
| 回测中心 | 单页配置 + 右侧运行摘要；指标可展开到交易明细；对比模式锁同日期同成本模型，数据版本不同必须出警告横幅 |
| 模拟执行 | 顶部时钟 + 暂停横幅（含生效时间，平仓仍可用）；部分成交行显示剩余数量；底部链路时间线（§4.7） |
| 账户与风险 | 持仓表区分总持仓/可用/T+1 冻结三列；限额编辑保存前显示影响范围预览 |
| 数据中心 | 数据集表格含来源/覆盖区间/缺失率/复权状态/许可证；补数只产生新版本，版本历史走抽屉 |
| 运行记录 | 运行 ID 等宽可点击；失败行提供可复制 CLI 重跑命令；重跑只建新运行 |

---

## 7. 可访问性底线

- 文本对比度 ≥ 4.5:1（现有 token 组合已满足，改动色值时需重新校验）。
- 状态不单独依赖颜色（图标 + 文案，见 §4.3）。
- 所有交互元素可键盘到达，聚焦态为 `1px var(--accent-blue)` 外描边。
- 表格支持键盘导航与行选择。

---

## 8. 给 Agent 的执行清单

实现或修改任何 ApexQuant 前端页面时，按顺序自检：

1. **Token 先行**：只用 §2 的 CSS 变量，新增色值/字号必须先补 token 再使用。
2. **禁项扫描**：对照 §1.4，确认产出没有渐变、大圆角、插画、装饰性语义色。
3. **数字合规**：所有数值等宽右对齐、符号与小数位符合 §2.2。
4. **状态完整**：每个数据面板都要能渲染 正常 / 加载 / 空 / 错误 四态；长任务带进度与取消。
5. **语义具体**：状态文案含具体数值与时间（「延迟 2 分钟」而非「异常」）。
6. **危险操作**：动词短语按钮 + 四要素确认层，缺一不可。
7. **可追溯**：任何收益数字能追到 策略版本 / 数据版本 / 成本模型 / 运行 ID；成交到审计链路 ≤ 2 次点击。
8. **宽度底线**：1440px 首屏无横向滚动；1180px 时主表格与风险栏完好。

